syntax = "proto3";

package api;

message Request {

    string requestID = 1;

    message DRKeyStoreGet {
        bytes drKey = 1;
        uint64 messageNumber = 2;
    }

    DRKeyStoreGet dRKeyStoreGet = 2;

}